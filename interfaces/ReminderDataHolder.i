HEADER_FILE := reminders\ReminderDataHolder.h
PARENT_FILE := DataHolder.h := true

MEMOWN := Reminder* getRef(long position) {

CONSTRUCTOR_IGNORE := ReminderDataHolder(std::vector<Reminder*>* reminders, ReminderFilter* filter);

IMPORT_HEADER := headers\ReminderWrapper.h

IMPORT_SOURCE := StringUtils.h